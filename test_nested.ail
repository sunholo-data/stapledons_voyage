module test/nested

-- Test file to verify nested if-else codegen fix
-- This should produce FLAT if-else statements, not nested closures

pure func classify(x: int) -> string {
    if x < 0 then "negative"
    else if x == 0 then "zero"
    else if x < 10 then "small"
    else if x < 100 then "medium"
    else if x < 1000 then "large"
    else "huge"
}

-- Entry point to test
func main() -> int ! {IO} {
    let result = classify(42);
    0
}
